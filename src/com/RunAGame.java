package com;
//import com.agent.Agent;
//import com.checkersgame.*;


/**
 * Main class
 */
public class RunAGame {
	/*public static void main(String ... args) throws Exception {
		CheckersGame checkersboard = new CheckersGame();
		boolean gameover = false;
		
		Agent player1 = null;//black
		Agent player2 = null;//red
		if(player1==null || player2==null) {
			throw new Exception("Not enough players");
		}
		
		//set the colors in the agents/players
		player1.SetColor("BLACK");
		player2.SetColor("RED");
		
		//let the game begin
		while(!gameover) {
			if(checkersboard.NumberOfBlackPieces()<=0 || checkersboard .NumberOfRedPieces()<=0 ) {
				gameover=true;
			}		
			//game is not over
		}
	}*/
}
